{{- range $client := .Values.application.oidc.clients }}
{{- $id := required "application.oidc.clients[].id is required" $client.id }}
{{- if contains "." $id }}
{{- fail "application.oidc.clients[].id must not contain a dot" }}
{{- end }}
{{ $id }}.description = {{ required "application.oidc.clients[].description is required" $client.description }}
{{- with $client.accessGroup }}
{{ $id }}.access-group = {{ . }}
{{- end }}
{{ $id }}.claims = {{ join " " (required "application.oidc.clients[].claims is required" $client.claims) }}
{{- if not (hasKey $client "signDocuments") }}
{{- fail "application.oidc.clients[].signDocuments is required" }}
{{- end }}
{{ $id }}.sign-documents = {{ $client.signDocuments }}
{{- end }}
